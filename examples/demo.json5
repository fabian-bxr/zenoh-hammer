{
  mode: "client",
  connect: { endpoints: ["tcp/127.0.0.1:7447"] },

  transport: {
    shared_memory: {
      enabled: true,
      transport_optimization: {
        enabled: true,
        pool_size: 67108864,
        message_size_threshold: 4096
      }
    }
  }
}
